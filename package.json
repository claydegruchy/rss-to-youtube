{
  "name": "lmao",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "watch": "nodemon --watch ./src/ --exec npm run start",
    "start": "npx @google-cloud/functions-framework --source=src/ --target=entry",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "@types/express": "^4.17.17",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "dependencies": {
    "ffmpeg-static": "^5.1.0",
    "fluent-ffmpeg": "^2.1.2",
    "googleapis": "^118.0.0",
    "rss-parser": "^3.13.0"
  }
}